from  tests.common import TestCase


class TestDummy(TestCase):

    def test_dummy(self):
        """
        A test that does nothing, so pytest doesn't complain that it found no
        tests to run.
        """
        pass
